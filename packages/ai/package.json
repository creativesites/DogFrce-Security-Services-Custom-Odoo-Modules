{
  "name": "@deployguard/ai",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AIProvider abstraction, Gemini implementation, and output validator. See README.md — infrastructure for V1, not an MVP feature.",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^0.3.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^1.6.1"
  }
}
