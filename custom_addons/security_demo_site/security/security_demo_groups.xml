<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Assigned to all non-admin demo users: blocks delete on operational models -->
    <record id="group_demo_no_delete" model="res.groups">
        <field name="name">Demo: No Delete</field>
    </record>

    <!-- Assigned to viewer only: blocks create + write + delete -->
    <record id="group_demo_viewer" model="res.groups">
        <field name="name">Demo: Read-Only Viewer</field>
    </record>
</odoo>
