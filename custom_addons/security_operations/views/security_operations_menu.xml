<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ─── Operations (quick-action landing) ─── -->
    <menuitem
        id="menu_security_operations_root"
        name="Operations"
        parent="security_base.menu_security_root"
        sequence="5"
        groups="hr.group_hr_user"
    />

    <!-- ─── Rostering ─── -->
    <menuitem
        id="menu_security_roster"
        name="Rostering"
        parent="security_base.menu_security_root"
        sequence="10"
        groups="hr.group_hr_user"
    />

    <!-- ─── Workforce ─── -->
    <menuitem
        id="menu_security_workforce"
        name="Workforce"
        parent="security_base.menu_security_root"
        sequence="20"
        groups="hr.group_hr_user"
    />

    <!-- ─── Payroll ─── -->
    <menuitem
        id="menu_security_payroll"
        name="Payroll"
        parent="security_base.menu_security_root"
        sequence="30"
        groups="hr.group_hr_user"
    />

    <!-- ─── Clients &amp; Sites ─── -->
    <menuitem
        id="menu_security_billing_root"
        name="Clients &amp; Sites"
        parent="security_base.menu_security_root"
        sequence="40"
        groups="hr.group_hr_user"
    />
</odoo>
